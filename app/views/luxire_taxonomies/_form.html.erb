<%= form_for(@luxire_taxonomy) do |f| %>
  <% if @luxire_taxonomy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@luxire_taxonomy.errors.count, "error") %> prohibited this luxire_taxonomy from being saved:</h2>

      <ul>
      <% @luxire_taxonomy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mega_menu_template %><br>
    <%= f.text_field :mega_menu_template %>
  </div>
  <div class="field">
    <%= f.label :spree_taxonomy_id %><br>
    <%= f.number_field :spree_taxonomy_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
