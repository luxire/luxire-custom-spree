<%= form_for(@luxire_style_master) do |f| %>
  <% if @luxire_style_master.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@luxire_style_master.errors.count, "error") %> prohibited this luxire_style_master from being saved:</h2>

      <ul>
      <% @luxire_style_master.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :default_values %><br>
    <%= f.text_field :default_values %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
